import React, { Component } from 'react';
import ParkInfo from './ParkInfoComponent';

class Main extends Component {
  render() {
    return (
      <div>
        <ParkInfo />
      </div>
    );
  }
}

export default Main;
